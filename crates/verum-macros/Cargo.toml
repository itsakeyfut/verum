[package]
name = "verum-macros"
description = "Derive and attribute macros for verum. Not intended to be depended on directly — use the `verum` crate"
version.workspace = true
edition.workspace = true
repository.workspace = true

[lib]
proc-macro = true

[dependencies]
proc-macro2 = { workspace = true }
quote = { workspace = true }
syn = { workspace = true }
